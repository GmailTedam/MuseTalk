# Turnkey clinician avatar for MuseTalk realtime lip-sync (section 5 PoC).
# Source video = real clinician talking-head (Dr Angela Adebayo, matches the
# female_dark persona whose source_portrait_path is already the same face).
# Driving audio = a real gpt-4o-mini-tts clip captured from the live avatar.
# Run on a CUDA box (see runbook): produces a lip-synced clinician clip.
clinician_female_dark:
  preparation: True
  bbox_shift: 5
  video_path: "data/video/clinician_female_dark.mp4"
  audio_clips:
    drive_0: "data/audio/clinician_drive.wav"
